const nombre = "Monitor 20 pulgadas";
const precio = 300;
const disponible = true;

//Ub objeto agrupa todo en una sola variable...

//Object literal
const producto = {
  nombre: "Monitor 20 pulgadas",
  precio: 300,
  disponible: false,
};

console.log(producto);
console.log(producto.nombre);

//otra forma de acceder a un atributo, poco usada
console.log(producto["nombre"]);
